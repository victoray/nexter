import React from "react";

const SideBar = () => {
    return (
        <div className="sidebar">
            <button className="nav__button">
                
            </button>
        </div>
    );
};

export default SideBar;